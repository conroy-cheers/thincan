[package]
name = "can-isotp-interface"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "ISO-TP transport traits shared across user-space and kernel backends"
repository = "https://github.com/conroy-cheers/thincan"

[dependencies]

[dev-dependencies]
