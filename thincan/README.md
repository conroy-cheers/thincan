# thincan

`thincan` is a small application-layer message protocol and router for ISO-TP based transports.

It provides message registry, bundle, and routing macros for building compact CAN/UDS-style protocols.
